package task;

/*
 * import java.time.Instant; import java.util.ArrayList; import java.util.List;
 * 
 * public class Epic extends Task {
 * 
 * private final List<SubTask> subTasks = new ArrayList<>(); private final
 * List<Integer> subTaskIds = new ArrayList<>(); private Instant endTime;
 * 
 * public Epic(String title, String description, TaskStatus status, TaskType
 * type) { super(title, description, status, type);
 * 
 * }
 * 
 * public Epic(String title, String name, TaskStatus status, Instant startTime,
 * long duration) { super(title, name, status, startTime, duration);
 * this.endTime = super.getEndTime();
 * 
 * }
 * 
 * public void putSubTask(SubTask task) { subTasks.add(task);
 * 
 * }
 * 
 * public ArrayList<SubTask> getSubTasks() { return (ArrayList<SubTask>)
 * subTasks; }
 * 
 * public void removeSubtask(SubTask subTask) { subTasks.remove(subTask);
 * 
 * }
 * 
 * public void setSubtaskId(int id) { subTaskIds.add(id); }
 * 
 * public Instant getEndTime() { return endTime; }
 * 
 * public void setEndTime(Instant endTime) { this.endTime = endTime; }
 * 
 * public List<Integer> getSubTaskIds() { return subTaskIds; }
 * 
 * }
 */